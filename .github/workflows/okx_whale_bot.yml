name: OKX 4H Smart Whale Bot

on:
  schedule:
    - cron: "0 */4 * * *"   # Her 4 saatte bir (00:00, 04:00, 08:00 ... UTC)
  workflow_dispatch: {}      # İstersen GitHub arayüzünden de elle çalıştırabilirsin

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CH_ID }}
        run: |
          python main.py
